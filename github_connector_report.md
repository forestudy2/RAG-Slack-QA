# GitHub 커넥터(GitHub CLI) 기능 및 사용 예시

본 문서는 사용자가 요청하신 GitHub 커넥터(본 환경에서는 **GitHub CLI**를 통해 통합됨)의 기능 설명과 실제 사용 예시를 보여드리기 위해 작성되었습니다.

## 1. GitHub 커넥터 개요

GitHub 커넥터는 사용자의 GitHub 계정에 안전하게 연결하여, 코드를 작성하거나 웹 브라우저를 열지 않고도 **명령줄 인터페이스(CLI)**를 통해 GitHub의 다양한 기능에 접근하고 데이터를 가져올 수 있도록 지원합니다.

이는 다음과 같은 이점을 제공합니다:
*   **자동화**: GitHub 작업을 스크립트나 자동화된 워크플로우에 통합할 수 있습니다.
*   **효율성**: 저장소, 이슈, 풀 리퀘스트 등 필요한 정보를 빠르고 간결하게 조회할 수 있습니다.
*   **통합 환경**: 현재 작업 중인 환경(샌드박스) 내에서 외부 서비스(GitHub)의 데이터를 즉시 활용할 수 있습니다.

본 환경에서는 `gh` (GitHub CLI) 명령어를 사용하여 이 커넥터 기능을 구현하고 테스트했습니다.

## 2. 핵심 기능 및 사용 예시

GitHub 커넥터는 저장소 관리, 이슈 및 풀 리퀘스트 처리, 사용자 정보 조회 등 GitHub의 핵심 기능을 지원합니다. 다음은 실제 테스트를 통해 가져온 데이터 예시입니다.

### 2.1. 사용자 계정 정보 조회

커넥터가 사용자의 GitHub 계정에 올바르게 연결되었는지 확인하고 기본 프로필 정보를 가져옵니다.

| 필드 | 설명 | 테스트 결과 |
| :--- | :--- | :--- |
| `login` | 사용자 ID | `forestudy2` |
| `id` | GitHub 내부 ID | `175711641` |
| `html_url` | 프로필 URL | `https://github.com/forestudy2` |
| `public_repos` | 공개 저장소 수 | `1` |
| `created_at` | 계정 생성일 | `2024-07-16T12:33:22Z` |

### 2.2. 저장소 목록 조회

사용자 소유의 저장소 목록을 조회하고, 각 저장소의 주요 메타데이터를 추출합니다.

**사용된 명령어:**
```bash
gh repo list --json name,description,stargazerCount,forkCount --limit 5
```

| 저장소 이름 (`name`) | 설명 (`description`) | Star 수 (`stargazerCount`) | Fork 수 (`forkCount`) |
| :--- | :--- | :--- | :--- |
| `RAG-Slack-QA` | (설명 없음) | 0 | 0 |

### 2.3. 특정 저장소의 커밋 기록 조회

특정 저장소의 최근 커밋 기록을 가져와 프로젝트의 진행 상황을 파악할 수 있습니다.

**사용된 명령어:**
```bash
gh api repos/forestudy2/RAG-Slack-QA/commits
```

| SHA | 커밋 메시지 (`message`) | 작성자 (`author`) | 날짜 (`date`) |
| :--- | :--- | :--- | :--- |
| `8e0d172a...` | Add files via upload | forestudy2 | 2024-09-08T09:50:36Z |
| `5aa44200...` | Delete src/main.py | forestudy2 | 2024-09-08T09:50:00Z |
| `616b9ec8...` | Add files via upload | forestudy2 | 2024-09-03T04:00:40Z |

### 2.4. 이슈 및 풀 리퀘스트 (PR) 조회

프로젝트의 협업 상태를 확인하기 위해 열려있는 이슈나 풀 리퀘스트 목록을 조회할 수 있습니다.

**테스트 결과:**
테스트 대상 저장소 (`RAG-Slack-QA`)에는 현재 열려있는 이슈나 풀 리퀘스트가 없었습니다. 데이터가 존재할 경우, 다음과 같은 정보를 가져올 수 있습니다:

*   **이슈**: 제목, 상태(열림/닫힘), 생성 시각 등을 조회하여 프로젝트의 버그나 기능 요청 사항을 추적할 수 있습니다.
*   **풀 리퀘스트**: 제목, 상태(열림/병합됨/닫힘), 생성 시각 등을 조회하여 코드 병합 요청 상태를 모니터링할 수 있습니다.

## 3. 기능 요약 및 활용 방안

GitHub 커넥터는 단순히 데이터를 가져오는 것을 넘어, GitHub에서 수행하는 거의 모든 작업을 CLI를 통해 처리할 수 있게 해줍니다.

| 기능 범주 | 주요 활용 명령어 (예시) | 활용 방안 |
| :--- | :--- | :--- |
| **정보 조회** | `gh repo list`, `gh issue list`, `gh pr list` | 프로젝트 상태 보고서 자동 생성, 특정 이슈/PR 검색 및 필터링 |
| **저장소 관리** | `gh repo clone`, `gh repo create` | 새로운 프로젝트를 신속하게 시작하거나 기존 저장소를 복제하여 작업 환경 설정 |
| **협업** | `gh issue create`, `gh pr checkout` | 새로운 이슈를 즉시 등록하거나, 풀 리퀘스트 브랜치로 빠르게 전환하여 코드 검토 |
| **고급 API 접근** | `gh api` | 표준 CLI 명령으로 지원되지 않는 GitHub의 모든 REST API 엔드포인트에 직접 접근하여 맞춤형 데이터 추출 |

이 커넥터를 활용하면 GitHub 데이터를 기반으로 한 **자동화된 보고서 작성**, **코드 분석 스크립트 실행**, 또는 **CI/CD 파이프라인 통합** 등을 효과적으로 수행할 수 있습니다.

---
*작성자: Manus AI*
*날짜: 2025년 10월 29일*
